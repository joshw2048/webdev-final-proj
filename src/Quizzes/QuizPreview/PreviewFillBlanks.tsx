import { Question } from "../types"

export interface PreviewProps {
  question: Question;
  questionNum: Number;
}

export const PreviewFillBlanks = (props: PreviewProps) => {
  return(
    <></>
  )
}